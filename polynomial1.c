#include <stdio.h>
#include <math.h>
int main(void){
	double x;
	double r;
	scanf("%lf",&x);
   printf("enter a value for x: %lf\n",x);
r= (3*(x*x*x*x*x))+(2*(x*x*x*x))-(5*(x*x*x))-(x*x)+7*x-6;
	printf("result: %lf\n",r);
		scanf("%lf",&x);
   printf("enter a value for x: %lf\n",x);
r= (((((3*x)+2)*x-5)*x-1)*x+7)*x-6;
	printf("result: %lf",r);
	
	
	
}